 * {
 	box-sizing: border-box;
 	/* Opera/IE 8+ */
 -moz-box-sizing: border-box;  Firefox, autres Gecko 
   -webkit-box-sizing: border-box; Safari/Chrome, autres WebKit
 	font-family: 'Raleway', sans-serif,'Roboto','Shrikhand';
 	margin: 0;
 	padding: 0;
 }
 
/*
.icone_heart_border {
        position: relative;
    z-index: 1;}

 .icone_heart_border:hover::after {
            opacity: 1;
    }
 .icone_heart::after { 
       content: "";
        position: relative;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
         color: radial-gradient(circle,
           #9356DC 0%, #FF79DA 100%);
            opacity: 0;
        z-index: -1;
        transition: opacity 250ms;
     
    }
    */
  .icone_border  {
      border-radius: 25px;
      width: 25px;
      font-size: 45px;
      padding: 30px 45px;
      border: 2px solid black;
           }
    .icone_border .icone_heart {
      border-radius: 25px;
      width: 25px;
      font-size: 45px;
      padding: 30px 45px;
     /* position: relative;
      z-index: 1;*/
      cursor: pointer;
     }
      .icone_border .icone_heart:hover {
        border-radius: 25px;
                     }
      .icone_border .icone_heart:hover::after {
  border-radius: 25px;
  opacity: 1;
}
      .icone_border .icone_heart::after {
        border-radius: 25px;
          content: "";
          color: radial-gradient(circle,
           #9356DC 0%, #FF79DA 100%);
   /*       position: relative;
           z-index: -1;
          top: 0;
          right: 0;
          bottom: 0;
          left: 0;


          
          opacity: 0;
         
          transition: opacity 250ms;*/
      }